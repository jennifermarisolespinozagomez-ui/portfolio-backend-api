# Ejemplo de variables de entorno para Render.com
# Render configurará estas automáticamente desde render.yaml

# Entorno de ejecución
ASPNETCORE_ENVIRONMENT=Production

# Puerto (Render lo asigna dinámicamente)
ASPNETCORE_URLS=http://0.0.0.0:$PORT

# Conexión a base de datos (Render la configura automáticamente)
ConnectionStrings__DefaultConnection=Host=dpg-xxxxx.oregon-postgres.render.com;Database=portfolio_db;Username=portfolio_user;Password=xxxxx

# CORS - Orígenes permitidos
AllowedOrigins__0=https://portfolio-frontend-weld-beta.vercel.app
AllowedOrigins__1=http://localhost:5173

# Notas:
# - No necesitas crear este archivo en Render
# - Render configura todo automáticamente desde render.yaml
# - Este archivo es solo para referencia
